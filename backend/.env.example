# ── Copia este archivo como .env y completa los valores ──

# Django
DJANGO_SECRET_KEY=cambia-esto-por-una-clave-segura
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=tudominio.com,www.tudominio.com

# PostgreSQL
DB_NAME=briane_db
DB_USER=briane_user
DB_PASSWORD=cambia-esto
DB_HOST=localhost
DB_PORT=5432

# CORS — dominio(s) del frontend en producción
CORS_ALLOWED_ORIGINS=https://tudominio.com,https://www.tudominio.com
